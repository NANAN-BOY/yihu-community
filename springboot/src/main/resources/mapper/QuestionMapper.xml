<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yihu.mapper.QuestionMapper">

    <select id="getQuestionList" resultType="com.yihu.entity.Question">
        select *
        from question
        where questionnaire_id = #{questionnaireId}
        order by questionId
    </select>

    <insert id="createQuestion" parameterType="com.yihu.entity.Question">
        insert into question(questionnaire_id, question_type, details, question_description, question_nullable,
                             question_title)
        values (#{questionnaireId}, #{questionType}, #{details}, #{questionDescription}, #{questionNullable},
                #{questionTitle})
    </insert>

    <delete id="deleteQuestion">
        delete
        from question
        where questionId = #{id}
    </delete>

    <select id="findQuestionByQuestionId" resultType="com.yihu.entity.Question">
        select *
        from question
        where question_id = #{questionId}
    </select>

</mapper>