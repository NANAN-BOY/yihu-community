<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yihu.mapper.ProductMapper">

    <insert id="addProduct" parameterType="com.yihu.entity.Product">
        INSERT INTO product (name, price, discount, proportion, create_user, create_at)
        VALUES (#{name}, #{price}, #{discount}, #{proportion}, #{createUser}, #{createAt})
    </insert>

    <delete id="deleteProduct">
        DELETE
        FROM product
        WHERE id = #{id}
    </delete>

    <select id="findProduct" resultType="com.yihu.entity.Product">
        SELECT *
        FROM product
    </select>

    <update id="updateProduct">
        UPDATE product
        <set>
            <if test="name != null and name != ''">
                name = #{name}
            </if>
            <if test="price != null and price != ''">
                price = #{price}
            </if>
            <if test="discount != null and discount != ''">
                discount = #{discount}
            </if>
            <if test="proportion != null and proportion != ''">
                proportion = #{proportion}
            </if>
            update_user = #{updateUser},
            update_at = #{updateAt}
        </set>
        WHERE id = #{id}
    </update>

</mapper>