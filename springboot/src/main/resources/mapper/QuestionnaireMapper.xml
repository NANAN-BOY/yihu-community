<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yihu.mapper.QuestionnaireMapper">

    <update id="deleteQuestionnaire">
        update questionnaire
        set delete_flag = 1,
            status      = 2,
            end_time    = NOW()
        where questionnaire_id = #{questionnaireId}
    </update>

    <insert id="create" parameterType="com.yihu.entity.Questionnaire" keyProperty="questionnaireId"
            useGeneratedKeys="true">
        insert into questionnaire
            (create_time, fill_count, status, delete_flag)
        values (#{createTime}, #{fillCount}, #{status}, #{deleteFlag})
    </insert>

    <update id="stopQuestionnaire">
        update questionnaire
        set status   = 2,
            end_time = NOW()
        where questionnaire_id = #{questionnaireId}
    </update>


    <update id="startQuestionnaire">
        update questionnaire
        set status     = 1,
            start_time = NOW()
        where questionnaire_id = #{questionnaireId}
    </update>

    <select id="selectByPrimaryKey" resultType="com.yihu.entity.Questionnaire">
        select *
        from questionnaire
        where questionnaire_id = #{questionnaireId}
    </select>

    <update id="incrementFillCount">
        update questionnaire
        set fill_count = fill_count + 1
        where questionnaire_id = #{questionnaireId}
    </update>

</mapper>