<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yihu.mapper.UserMapper">

    <select id="selectAll" resultType="com.yihu.entity.User">
        SELECT * FROM user
    </select>

    <insert id="register" parameterType="com.yihu.entity.User">
        INSERT INTO user (
            name, phone, password, role, status, balance, create_at, last_login_time, updater_id,
                          update_at, delete_flag, location
        ) VALUES (
                     #{name}, #{phone}, #{password}, #{role}, #{status}, #{balance}, #{create_at},
                  #{last_login_time}, #{updater_id}, #{update_at}, #{delete_flag}, #{location}
                 )
    </insert>

    <select id="login" resultType="com.yihu.entity.User">
        SELECT * FROM user WHERE phone = #{phone}
    </select>

</mapper>
