<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yihu.mapper.ActivityMapper">
    <insert id="create" parameterType="com.yihu.entity.Activity" useGeneratedKeys="true" keyProperty="id">
        insert into activity(
        status, del_flag, create_time, create_by_id, update_time, update_by_id
        )
        values(
        #{status}, #{delFlag}, #{createTime}, #{createById}, #{updateTime}, #{updateById}
        )
    </insert>



    <delete id="deleteById" parameterType="com.yihu.entity.Activity">
        update activity
        set del_flag = 'Y',
            update_time = #{updateTime},
            update_by_id = #{updateById}
        where id = #{id}
    </delete>

    <update id="update" parameterType="com.yihu.entity.Activity">
        update activity
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="noticeContent != null">notice_content = #{noticeContent},</if>
            <if test="staffCount != null">staff_count = #{staffCount},</if>
            <if test="volunteerCount != null">volunteer_count = #{volunteerCount},</if>
            <if test="serviceObjectCount != null">service_object_count = #{serviceObjectCount},</if>
            update_time = #{updateTime},
            update_by_id = #{updateById}
        </set>
        where id = #{id}
    </update>


    <select id="queryByCreateId" parameterType="com.yihu.entity.Activity">
        SELECT *
        FROM activity
        WHERE create_by_id = #{createById} AND del_flag = 'N'
        <if test="title != null and title != ''">
            AND title LIKE CONCAT('%', #{title}, '%')
        </if>
    </select>

    <update id="submit" parameterType="com.yihu.entity.Activity">
        update activity
        set status = #{status},
        update_time = #{updateTime},
        update_by_id = #{updateById}
        where id = #{id}
    </update>

    <select id="findById" resultType="com.yihu.entity.Activity">
        SELECT *
        FROM activity
        WHERE id = #{id} AND del_flag = 'N'
    </select>

    <update id="withdrawSubmission" parameterType="com.yihu.entity.Activity">
        update activity
        set status = 0,
        update_time = #{updateTime},
        update_by_id = #{updateById}
        where id = #{id}
    </update>

    <select id="queryAllSubmited" parameterType="com.yihu.entity.Activity">
        SELECT *
        FROM activity
        WHERE status = 1 AND del_flag = 'N'
        <if test="title != null and title != ''">
            AND title LIKE CONCAT('%', #{title}, '%')
        </if>
    </select>


</mapper>
