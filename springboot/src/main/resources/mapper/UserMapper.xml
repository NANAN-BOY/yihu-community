<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yihu.mapper.UserMapper">

    <select id="selectAll" resultType="com.yihu.entity.User">
        SELECT * FROM user
    </select>

    <insert id="register" parameterType="com.yihu.entity.User">
        INSERT INTO user (
            name, phone, password, role, status, balance, create_at, last_login_time, updater_id,
                          update_at, delete_flag, location
        ) VALUES (
                     #{name}, #{phone}, #{password}, #{role}, #{status}, #{balance}, #{createAt},
                  #{lastLoginTime}, #{updaterId}, #{updateAt}, #{deleteFlag}, #{location}
                 )
    </insert>

    <select id="login" resultType="com.yihu.entity.User">
        SELECT * FROM user WHERE phone = #{phone}
    </select>

    <select id="selectByPhone" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0 FROM user WHERE phone = #{phone}
    </select>

    <update id="resetPassword">
        UPDATE user SET password = #{newPassword} WHERE phone = #{phone}
    </update>

    <select id="getUserInfo" resultType="com.yihu.entity.User">
            SELECT name,location FROM user WHERE id = #{userId}
    </select>

    <update id="updateUserInfo">
        UPDATE user
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="location != null and location != ''">
                location = #{location},
            </if>
            updater_id = #{id},
            update_at = #{updateAt}
        </set>
        WHERE id = #{id}
    </update>

    <select id="query" resultType="com.yihu.entity.User">
        SELECT * FROM user
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone LIKE CONCAT('%', #{phone}, '%')
            </if>
            <if test="role != null">
                AND role = #{role}
            </if>
            <if test="location != null and location != ''">
                AND location LIKE CONCAT('%', #{location}, '%')
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <update id="banUser">
            UPDATE user SET status = 1, updater_id = #{updateId}, update_at = #{updateTime} WHERE id = #{userId}
    </update>

    <update id="unbanUser">
            UPDATE user SET status = 0, updater_id = #{updateId}, update_at = #{updateTime} WHERE id = #{userId}
    </update>

    <update id="updatePhone">
        UPDATE user
        SET phone      = #{newPhone},
            updater_id = #{userId},
            update_at  = #{updateTime}
        WHERE id = #{userId}
          And phone = #{oldPhone}
    </update>

</mapper>
