<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css"/>
    <title>项目申报管理</title>
</head>
<body>
        <aside>
            <h1>智能管理平台Beta</h1>
            <ul>
                <!-- <li><a href="/userdashboard">首页</a></li> -->
                <li><a class="onclick" href="/user/manage-project">项目管理</a></li>
                <li><a href="/logout">安全登出</a></li>
            </ul>
        </aside>
    <div class="container">
        <div class="manage-projects">
            <h1>项目申报管理</h1>
            <div class="button-container">
                <a href="/project-declaration" class="btn create">填写项目申报</a>
            </div>
            <table>
                <thead>
                    <tr>
                        <th>申报ID</th>
                        <!-- <th>模板名称</th> -->
                        <th>提交时间</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody id="projectTableBody">
                    <% if (entries && entries.length > 0) { %>
                        <% entries.forEach(entry => { %>
                            <tr id="project-<%= entry.entry_id %>">
                                <td onclick="viewProject(<%= entry.entry_id %>)"><%= entry.entry_id %></td>
                                <!-- <td onclick="viewProject(<%= entry.entry_id %>)"><%= entry.template_name %></td> -->
                                <td onclick="viewProject(<%= entry.entry_id %>)">
                                    <%= entry.submitted_at ? new Date(entry.submitted_at).toLocaleString() : '暂无时间' %>
                                </td>
                                <td>
                                    <button class="btn view" onclick="viewProject(<%= entry.entry_id %>)">查看详情</button>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="4">暂无申报记录</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
    <script>
        // 查看单个项目申报
        function viewProject(entryId) {
            window.location.href = '/view-project/' + entryId;
        }
    </script>
</body>
</html>
